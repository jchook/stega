{
  "name": "stegapng",
  "version": "1.2.1",
  "description": "Steganography for PNG images",
  "main": "index.js",
  "scripts": {
    "fmt": "prettier --write packages/*/src/**/*.ts",
    "prepack": "pnpm run -r prepack",
    "test": "pnpm run -r test",
    "version": "pnpm -r version"
  },
  "keywords": [
    "steganography",
    "png"
  ],
  "author": "Wes Roberts <takq@a.zinc.email>",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-istanbul": "^1.0.4",
    "husky": "^8.0.3",
    "prettier": "^3.1.1"
  }
}
