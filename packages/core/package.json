{
  "name": "@stegapng/core",
  "version": "1.0.0",
  "description": "Hide secret data in images",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm run build:src && pnpm run build:docs",
    "build:docs": "typedoc",
    "build:src": "tsc",
    "clean": "rimraf dist docs",
    "dist": "pnpm run clean && pnpm run build && pnpm run test run",
    "precommit": "pnpm run dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "rimraf": "^5.0.5",
    "typedoc": "^0.25.4",
    "typedoc-plugin-markdown": "^3.17.1",
    "typescript": "^5.3.2",
    "vitest": "^1.0.1"
  },
  "license": "MIT"
}
